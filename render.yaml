services:
  - type: web
    name: easytransfer
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: STORAGE_ROOT
        value: /data
    disk:
      name: easytransfer-data
      mountPath: /data
      sizeGB: 1
